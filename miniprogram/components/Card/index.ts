Component({
	options: {
		multipleSlots: true
	},
	properties: {
		width: String,
		padding: String,
		margin: String,
            radius: Number,
		fontSize: Number,
		color: String,
		bgColor: String,
		textAlign: String,
		bordered: Boolean,
		shadow: Boolean,
		style: String
	}
})
