<view
      id="ripple"
      class="ripple_wrap"
>
      <view
            class="ripple"
            style="{{styles}}"
            wx:if="{{visible}}"
      ></view>
      <view
            class="content"
            catchtap="trigger"
      >
            <slot></slot>
      </view>
</view>