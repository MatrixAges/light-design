<view
      class="fixed_dialog_wrap"
      catchtouchmove="catchtouchmove"
      style="z-index:{{zIndex}};"
      wx:if="{{_visible}}"
>
      <view
            class="dialog"
            style="{{style}}"
      >
            <view class="content_wrap {{theme}}">
                  <view class="left">
                        <text class="title {{description?'has_description':''}}">{{title}}</text>
                        <text
                              class="description"
                              wx:if="{{description}}"
                        >{{description}}</text>
                  </view>
                  <view class="right">
                        <button
                              class="btn_confirm"
                              catchtap="onOk"
                        >{{okText}}</button>
                  </view>
            </view>
      </view>
</view>