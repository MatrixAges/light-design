Component({
	options: {
		multipleSlots: true
	},
	properties: {
		p: Boolean,
		b: Boolean,
		i: Boolean,
		del: Boolean,
		h1: Boolean,
		h2: Boolean,
		h3: Boolean,
		h4: Boolean,
		h5: Boolean,
		h6: Boolean,
		h7: Boolean,
		quote: Boolean,
		success: Boolean,
		warning: Boolean,
		error: Boolean,
		color: String,
		fontSize: String,
		textAlign: String,
		style: String,
		lineClamp: Number
	}
})
